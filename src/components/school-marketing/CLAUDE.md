---
epic: 14
sprint: Q3-2026
title: School Marketing
file_type: claude
owner: Samia
maturity: Built+Polish
completion: 93
tracker: https://github.com/databayt/hogwarts/issues/327
docs: https://databayt.org
last_audited: 2026-06-13
---

# School Marketing Block

## Context

Public-facing school website on subdomain (e.g., `demo.databayt.org`). Includes homepage sections, admission portal, multi-step application form, visit booking, academic pages (93% complete). No blockers.

## Before You Start

1. Read `README.md` here for full file structure and key patterns
2. Read `ISSUE.md` here for priorities
3. If working on admission/application, also read the cross-block rule at `.claude/rules/blocks/admission.md`

## Key Decisions

- **`/academic` borrows zenda's compositions but is rebuilt native (2026-08-06)**: the last page still speaking the old language (Geist 900 hero, shadcn buttons) now matches the chrome. It takes **zenda's /for-schools hero entrance** and **its /about-us values accordion**, but as native Tailwind + dictionary keys, not `.zenda-clone` markup — same call `/admissions` made, and for a reason worth recording: **a mixed clone/native page is not possible.** The CSS scoping is clean (every bare-tag rule in `zenda-clone.css` is `.zenda-clone`-prefixed, zero unprefixed, so a per-section wrapper leaks nothing), but the cloned sections are entirely rem-based and zenda drives rem off a viewport-derived `html{font-size}` (`ZendaRootScale`). `rem` resolves against the document root and **cannot be class-scoped**, so it is all-or-nothing: render the scale and every native section rescales with it, or omit it and the cloned section runs ~20% large below 1440px. Hence: port the composition, never wrap the section.
  - **Hero** (`academic/sections/hero.tsx` + `hero-kinetic.tsx`): the /for-schools entrance — a fixed splash of three circles on a `difference` blend holds 1.5s and fades, while every WORD of the headline flies in from a scattered start (scale 2.4–3.3, ±400–880%) and settles over ~2s on `[0.33,1,0.68,1]`, and a purple panel behind the last word retracts 108%→0. Verified live by rAF-sampling computed transforms, not by screenshot. **The scatter is derived from the word index, not authored per word** — the reference hard-codes an offset per word and would break the instant the copy changed; ours survives any headline in either language. **Line breaks, unlike the scatter, ARE authored**: a `\n` in `academic.hero.title` becomes a real `.kinetic-break` (a `flex-basis:100%` spacer), so a translation picks its own turn instead of inheriting wherever English ran out of measure. Dropped from the reference: the 16vh hero lift (it exists to make room for the building, but the reference's building is a fixed overlay that ignores the lift, so all the lift does is jolt the settled words — ours reserves the building's box from the first frame) and `reducedMotion="never"` (a school site honors the preference — under `reduce` the words are simply there). **No CTA, deliberately**: the reference has none, the application lives one nav item away on `/admissions`, and a pill here competed with the entrance at exactly the moment the words were still converging.
  - **The 2026-08-06 for-schools parity pass** took the three things the first pass had dropped, and they only work as a set: **hero → marquee → intro card**, in that order, because the building leaves the hero, crosses the marquee, and lands in the card. Put a section between them and the flight passes behind it.
    - **Building + stars art** = `public/lottie/school-header.json`, copied from `~/zenda/public/lottie/` (223KB; `public/lottie/` was already the pattern here — `institutes.json` predates it). This is the asset the first pass could not find: it is NOT on `cdn.databayt.org`, and the dead URL it replaced pointed at Webflow's CDN, which answers **403** to us. The gold four-point stars are part of this Lottie, not separate art. Rendered at its own colors — no recolor pass like `/admissions` does, since it already ships purple-on-line-drawing.
    - **The merge** (`sections/hero-art.tsx`): a fixed copy of the box lerps `left/top/width/height` from the hero placeholder's live rect into `[data-hero-art-target]` (the intro card's slot), driven by scroll. **Sizing the box, not transforming it, is the whole trick** — it lets the aspect change from the hero's 2:1 letterbox to the card's 3:2 slot without distorting the drawing. It lands at `SHRINK 0.85` grounded at the slot's foot (`EXTRA_DROP 14`), zenda's numbers. Two details that look optional and are not: it runs on a **continuous rAF, not a scroll listener** (the placeholder is still moving during the entrance, which fires no scroll events), and the flyer is **portalled to `<body>`** — the hero carries `overflow: clip` for the word scatter and would otherwise clip the building mid-flight. Under `reduce` there is no flyer and no merge at all: the art simply renders in the hero, and a media query collapses the card's now-empty slot.
    - **Marquee** (`sections/marquee.tsx` + `.mq-*`): three rows of school-subject words, middle row reversed, converging in place (scale 1.6→1, opacity 0→0.4 — **no translateY**, or the bottom row arrives visibly late). Each row is **two identical copies** sliding exactly `-50%`; anything other than two equal copies and the loop tears. The track is pinned `direction: ltr` (row order is decorative, and under RTL the flex row lays the duplicate on the wrong side) while each `.mq-word` gets `unicode-bidi: isolate` so Arabic still shapes correctly inside its own box. Full-bleed via `margin-inline: calc(50% - 50vw)`, never `100vw` — vw overshoots by the scrollbar width. Words come from `academic.marquee.words` (12, bilingual).
    - **Intro card** (`sections/intro.tsx` + `.acad-intro_*`): zenda's `.schs-zenda_*`. The slot's `top: -13rem` is a **visual** offset (layout still reserves the box) and the content's `margin-top: -16rem` is what closes the gap it leaves — the two are tuned to each other, move them together or not at all. Its copy is the programs section's own header, **moved rather than invented**; `programs.tsx` no longer renders one, because two adjacent sections introducing "Academic Programs" read as a stutter.
    - **The nav tuck is now real** — see the zenda-nav note below. Verified at 1440 against `~/zenda` on :3100: nav 85px, page rests at scrollY 85, h1 640×134 at 56px/500/-2.24px DM Sans, lede 544 wide at 20px, hero art box 1152×576, marquee section 336 tall with 30px words, card 1056 wide, slot 640×427 — every number identical. No horizontal overflow at 1440 or 390, in either language.
  - **Curriculum** = the values accordion, ported to `.stage-*` in `school-marketing.css` + `sections/stage-accordion.tsx`. One stage open at full width, the others collapsed to a numbered rail with a vertical label. **Four stages since 2026-08-06** — Kindergarten / Elementary / Middle / High — which is also the reference's own card count, so the collapsed rail (3 × 8rem + gaps) and the open card were sized against it. "Early Years (K-5)" used to collapse kindergarten and elementary into one card; they are different rooms with different teaching. Icons are **zenda's four filled glyphs**, reused straight from `/images/about/values/` (not copied and renamed — one set of four in one place; the corollary is that re-arting `/about`'s values re-arts this section). They are loaded **eagerly on purpose**: three of the four cards are `display: none` until opened, and a lazy image in a display:none subtree is never fetched, so each glyph would pop in on first hover. `.stage-card_icon` declares `aspect-ratio: 1` because all four files are square and `height: auto` on an undecoded image measures 0, which made the heading jump on arrival. **The vertical label's RTL behaviour is not what this note used to claim** — measured per glyph with Range rects (2026-08-06), `writing-mode: vertical-lr` + `rotate(180deg)` reads Latin **bottom-to-top** (the ordinary Western vertical-label convention, and what zenda.com renders) and Arabic top-to-bottom. Both are legible; the pair is kept because it is byte-identical to `.about-values_text-vertical`. Measure before believing any claim about it. The `.stage-card` stacking breakpoint (768) and the `StageAccordion` bail-out must not drift apart.
  - **`.zenda-heading` needed modifiers, not utilities.** It pins `font-size` and `color`, and lands in the same `@layer utilities` as Tailwind at equal specificity — so `zenda-heading text-3xl` or `zenda-heading text-white` is a coin-flip decided by emitted order, and a section h2 would silently render at the hero's 4.4rem. Use `.is-section` (zenda's `.heading-style-h2` ramp) and `.is-inverse` (for the charcoal CTA panel).
  - Also fixed in passing: the CTA's "Schedule a Visit" pointed at `/application`; it now points at `/tour`. `academic/page.tsx` gained the same route-scoped rail + cream `<style>` that `/admissions` carries, so content shares a vertical with the nav logo. **The stats row is still four invented figures** (95% acceptance, 98% graduation) on a page that renders for every tenant — marked PLACEHOLDER in-file and deliberately left un-animated so a count-up does not draw the eye to the numbers we can least back. Give it the admissions treatment (only promises the school controls) or drop it before a real tenant publishes.

- **Homepage is a verbatim zenda.com clone (2026-08-03)**: the tenant homepage is now a copy-paste of zenda.com's homepage — `src/components/school-marketing/zenda-home/` (8 sections + 5 GSAP scroll drivers), with `src/components/template/zenda-nav/` and `zenda-footer/` as the chrome for the whole block, replacing `SiteHeader`. Copy and imagery are zenda's, unchanged and in English; the deviations are all in the chrome, all requested: the hero's EXPLORE pill is relabelled "Apply" and points at `/application` (it was briefly two pills, `/tour` + `/admissions`, until 2026-08-05 — one action converts better on a school homepage, and `/application` is the canonical entry path used everywhere else in the block; there is no `/apply` route); nav links come from `marketingConfig.mainNav`; the logo is a small mark with the school name beside it (zenda's is a 10.8rem wordmark that needs no label); links and hamburger group at the end of the bar; and the hamburger is on at every width, opening a panel that holds the search / language / theme / account controls inherited from `SiteHeader`. Those nav overrides live in `zenda-shell.css` and **must** carry a `.zenda-clone` prefix — the generated sheet styles the nav at `.zenda-clone .nav_*`, so a bare selector loses on specificity regardless of load order. The previous real-school template (`content.tsx` + its 16 sections) is untouched on disk and still serves every sibling page — do not delete it. **Three constraints govern any edit here:**
  1. **`.zenda-clone` must never wrap `<main>`.** `src/styles/zenda-clone.css` carries unlayered bare-tag rules (`.zenda-clone a`, `h1`, `p`, `button`, `img`, `label`) and unlayered CSS beats Tailwind's `@layer utilities`, so any sibling page under that class gets silently restyled with no opt-out. The class sits on three small wrappers only: the nav, the footer slot, and the homepage body.
  2. **The page shell is hand-ported unscoped** into `src/styles/zenda-shell.css` (`.page-wrapper`, `.main-wrapper`, `.zenda-footer-slot`, `.nav_utility-wrap`) for exactly that reason — the scoped originals at `zenda-clone.css:24193-24238` would force the class onto an ancestor of `<main>`. Regenerating `zenda-clone.css` does not regenerate the shell; keep them in step by hand.
  3. **English/LTR only.** The Webflow CSS has no RTL handling, so the three wrappers force `dir="ltr"` and `/ar` renders the same English page. `<html dir>` is untouched, so the RTL assertions on sibling pages still hold.
  4. **Three things carry the clone's typography, and each failed silently at first.** All three were found by measuring against zenda running locally, not by looking at screenshots — the page looked plausible while being wrong. Verified fixed: section geometry is now identical to zenda at 1440px and 390px, total page height included.
     - **DM Sans and Poppins come from `next/font`** (`src/components/template/zenda-fonts.ts`), not from the `@import url(fonts.googleapis.com/…)` the generator used to emit. Next concatenates `zenda-clone.css` after `globals.css`, and an `@import` that is not at the top of the resulting stylesheet is invalid and gets dropped — so it never worked and the whole clone rendered in system Helvetica. They are deliberately **not** in `@/components/atom/fonts`: the root layout imports that module, which would ship both families on every page in the app.
     - **The fluid root scale must sit on `html`** (`zenda-home/root-scale.tsx`). Zenda sizes everything in `rem` off a viewport-derived root font size; the scope script rewrites `html` → `.zenda-clone`, and `rem` resolves against the document root, never an ancestor, so the whole system was inert. The clone matched only at ≥1440px — at 1200px every rem-based size was ~20% too large. It cannot be class-scoped, so it is rendered from the page and applies to the homepage alone.
     - **No `LoadingWrapper` on this block.** It held the page at `visibility: hidden` behind a full-screen "0%…100%" counter for ~2s, then faded it in from `scale(0.98)`. Zenda has no preloader, and the two fought: the hero's GSAP intro starts on mount, so it played its whole 1.8s centred hold while still hidden and the page arrived mid-split, 2% undersized and offset ~98px down. The zenda intro is the entrance here. Re-adding it silently breaks the first screen.
     - **The intro's logo travel is measured, not the reference's `x: "318%"`.** That percentage is of the element's _own_ width, so it only centres zenda's 173px wordmark. `hero-intro.tsx` computes the distance to viewport centre instead, which holds for any logo. Anything that should be absent during the centred splash needs a `hero-link` attribute to join the staggered fade — the hamburger carries one for exactly this reason.
     - **The page is gated at `opacity: 0` until the intro stages its first frame**, or the settled layout paints for ~300ms and then visibly snaps back to the splash. The reference puts `style={{opacity:0}}` on its own `.page-wrapper`; ours is in the shared layout, so the gate is a rule rendered from `page.tsx` and exists on the homepage alone. `HeroIntro` clears it and carries a 2.5s failsafe — this is a public homepage, and "hidden until JS says otherwise" must not survive a thrown error. Sibling pages have no `HeroIntro`, so the gate must never move into the layout.
     - **The nav's scroll-tuck is OPT-IN, and must stay that way** (reinstated 2026-08-06 — this note previously said the listener was dropped; `/academic` now ports for-schools and needs it). The reference answers `zenda:hero-mounted` with `window.scrollTo(0, navHeight)` so that hero loads flush to the top edge with the nav parked just above the fold until the first upward scroll. A listener whose whole job is to scroll the page down is a landmine on any page that did not ask, so there are **two triggers and both are opt-in**: `[data-zenda-tuck]` present in the DOM at nav mount (server-rendered by the academic hero, so it is already there on a fresh load) and the `zenda:hero-mounted` event (for a client-side navigation, where this nav does not remount). Neither is gated on `prefers-reduced-motion` — the tuck is an instantaneous scroll POSITION, not an animation, and gating one trigger and not the other would leave a fresh load and a soft navigation disagreeing about where the page starts. **The homepage marks neither and still opens at scrollY 0 with its logo centred; `/admissions` likewise** — verified after the change.
     - **`scripts/scope-zenda.mjs` concatenates globals.css FIRST, then the Webflow sheet.** Measured on zenda: its Next bundle is `document.styleSheets[0]` and the Webflow CDN `<link>` is `[2]`, so the CDN loads last and wins ties — which is why zenda's globals.css reaches for `!important` twenty times. The original order inverted the cascade for every tie; correcting it also halved `.section_services` on `/features/[id]`, which had been rendering at 7225px against zenda's 3595px.
- **SCHOOLS section is the school's, logos still owed (2026-08-06)**: the section was zenda's vendor pitch on the tenant's own homepage — "Trusted by 450+ institutions / for managing payments and financial operations", then "As the school's **fintech partner**, **zenda** offers financial services…" (naming zenda twice), with Explore pointing at `/for-schools`, a 404 here. It now reads "Everything in one place" / "for families, teachers and everyone between" / "Enrolled. Informed. Involved." / "From the first application to the last report card, the school keeps one record of every child, and gives families a window onto it, any hour" — 4 / 6 / 3 / 26 tokens, matching the reference exactly, and CTA → `` `/${lang}/about` `` (so `Schools` takes `lang` from `HomeContent`, like `Hero` and `Rewards`). **Deliberately claim-free**: a vendor can say "450+ institutions", a single school has no countable equivalent, and wiring live tenant counts (students/teachers/classes) was considered and deferred — so the copy asserts only what ships (application wizard, student record, parent portal). **⚠️ STILL OWED: the twelve real institution logos** (Arbor, Amity Abu Dhabi, Nord Anglia, Repton, North Collegiate, Raffles + six unnamed) are zenda's customers and still render, flagged in-file — they claim relationships the school doesn't have and must be swapped or deleted before a real tenant publishes. The four `<rect> transform: matrix(NaN…)` console errors come from this section's Institutes Lottie (`/lottie/institutes.json`, served locally) and predate all of this.
- **REWARDS section → DISCOUNTS (2026-08-06)**: the section sold zenda's loyalty program — "Unlock new experiences when you **pay on zenda**", "turn **fees** into a little extra joy by earning **gift cards**" — and, worse than the words, floated **three real brand logos (Amazon, talabat, noon)** as reward coupons plus Ulta/Givingli gift-card images, so every tenant's homepage implied reward partnerships that don't exist. It now describes the discounts a school actually configures: the header reads "Pay less for siblings, early birds and scholars" / "Register early, enroll a second child or win a scholarship — every discount the school offers comes off your fees", at the reference's exact token counts (8 and 20). **Match the reference's character count, not just its word count** — a first pass ("Discounts for siblings, early registration and scholarship students") held the 8 words but ran 67 characters against zenda's 44, and the display-scale h2 turned it into a wall; the version that shipped is 47. The globe chip is sized for a points figure ("1,250") at 4/3/2rem, so `zenda-shell.css` steps it down to 3/2.25/1.5rem at zenda's own 991/767 breakpoints now that it reads a word. **The three cases are the real ones** — `onboarding/discount/config.ts` ships `early_bird`, `sibling`, `scholarship` next to plain percentage/fixed. Worded as "the school offers", never "automatically": a discount lands on a student's fee when it is assigned (`StudentFee.discounts` / `totalDiscount`), nothing computes it on its own. **The brand artwork is deleted and must not come back** — `.rewards_coupons-wrap` and `.rewards_bg-img-wrap` are gone from `rewards.tsx`, with their `[rewards-item-*]` / `[rewards-img-*]` tweens removed from `rewards-scroll.tsx` (the survivors sit at absolute `0` / `0.2`, so nothing downstream shifted; globe, curve, seven blocks, two coins and the gift still animate — verified by sampling computed transforms across the ScrollTrigger). The CTA pointed at zenda's `/parents`, which 404s here, and now goes to `` `/${lang}/admissions` `` — which is why `Rewards` takes `lang` and `HomeContent` passes it, exactly as `Hero` does. Class and attribute names stay `rewards_*`: they are generated-sheet selectors and GSAP targets, only the words changed. Pre-existing and unrelated: `.section_schools` logs four `<rect> transform: matrix(NaN…)` console errors.
- **"You juggle life" section is the school's (2026-08-06)**: `zenda-home/phone-mockup.tsx` sold zenda's fees app on every tenant's homepage — "we handle **fees**", "pay and track all **transactions**". Now "You juggle life—we handle school" / "Discover, apply, enroll, track lessons, grades and attendance in one place", written to the reference's exact word counts (6 and 11) so the header keeps its line breaks and the `features-scroll` assembly is untouched. **"pay" is deliberately out of the verb list** — applying is always free in this block and fees only arrive after an offer is accepted, so a verb list promising payment here would contradict the wizard. The same sentence in `marketing.phoneMockup.subheading` (en + ar) was updated in step; that dictionary twin feeds the older non-clone `school-marketing/phone-mockup.tsx`, which no route currently renders.
- **Footer wordmark + APPLICATION pill (2026-08-06)**: the footer's zenda logo image (a Webflow CDN PNG) is replaced by the school name, set exactly as the nav sets it — `.footer_logo-text` shares the `.nav_logo-text` rule in `zenda-shell.css`, matching the reference where the footer logo is the same 172.8×38 image as the nav's. `Footer` now takes `displayName` + `locale` from the layout (its home link was a bare `/`, which drops the locale). The hero pill reads **APPLICATION** (was "Apply"); its `data-text` is the rolled duplicate and must match the visible text exactly. **Still zenda's, and a tenant publishes it:** the footer's two paragraphs describe zenda the fintech, the link columns point at zenda routes (`/for-schools`, `/blogs`, `/institution-terms-conditions-…`), and the three social links go to zenda's real Instagram/LinkedIn/Facebook.
- **Nav is a wordmark; admissions hero is black-and-purple (2026-08-06)**: the nav lockup dropped the crest and is now the school name alone, mirroring zenda's wordmark — 1.75rem/700 in zenda purple `#ac6cf4` (1.375rem below 768px, since a school name is arbitrarily longer than "zenda"), styled in `zenda-shell.css`. `ZendaNav` still accepts `logoUrl` and `.zenda-clone .nav_logo-link img` still sizes a crest at 2rem, so putting one back is markup-only. The admissions hero's Lottie recolor (`admission/sections/hero-illustration.tsx`) now swaps **only** clay `#d97757` → `#ac6cf4` and leaves the asset's near-black linework alone; mapping the ink to `--secondary--dark-purple` had made the whole illustration monochrome purple.
- **PARENTS VOICE peek + tab title (2026-08-06)**: two small parity fixes. (1) The testimonial slider now shows the previous/next cards flanking the centred one, as the reference does — the Swiper config was already right; what hid them was a local override in `src/styles/apple-clone.css` that set `overflow: hidden` on `.testimonial_swiper_wrap` and `.testimonial_swiper_component`. Those must stay `visible`; the peek is clipped by `.section_testimonial` (`overflow: hidden`, full-width) and that alone is what keeps the page from scrolling sideways — verified `documentElement.scrollWidth === innerWidth` at 1440. Neighbour geometry now matches `~/zenda` on :3100 exactly (prev 0→143px, next 1297→1440px). (2) `generateSchoolMetadata` resolved the tab title from `school.name`/`nameEn` directly, so a school with no `nameEn` kept an Arabic `<title>` on `/en`; it now calls the same `resolveSchoolDisplayName` the nav uses, which falls through to on-demand translation. The nav hamburger's bars were also slimmed (`--width: 68%`, `--gap: 0.42rem`) — those vars exist only in the inline `<style>` in `zenda-nav/header.tsx`, nothing in the generated sheet defines them.
- **About page copy is the school's; the UI is still zenda's (2026-08-05)**: a second pass rewrote every user-visible string on `/about` into the school voice — hero, what-we-do, growth journey (cities → grade milestones), team, people, mission, values, events — while leaving every class, attribute, image and GSAP/Swiper timeline byte-identical. Two rules shaped it. (1) **Offices, not people.** The two portrait cards are "Office of the Principal" and "Our Teaching Faculty", never a named principal: this page renders for every tenant, so a named person with a bio is an invented human on somebody's real school site — the same rule that de-Pottered the homepage. (2) **Nothing asserted that the page can't back.** Milestones, the twelve events, and the class figures are marked PLACEHOLDER in-file, in the same spirit as the demo homepage's stats. External links were repointed inward (`/${lang}/tour`, `/inquiry`, `/academic`) because the originals went to zenda's real LinkedIn posts and profiles; that needed `lang` threaded through `AboutContent` → `Makers`/`Events`. **Three imagery blockers remain and are commented as such in-file — they cannot be fixed with copy:** `investors/*` are four real VC logos, `trusted/*` are four real accrediting councils (a fake accreditation claim, which this block explicitly bans), and `team/*` includes a club-lit party and a billiards table under "Our Team". Swap or delete before any real school publishes; the `raman`/`haseeb` portraits are real identifiable people too.
- **About page is a verbatim zenda.com/about-us clone (2026-08-05)**: `/about` renders `src/components/school-marketing/zenda-about/` (10 sections, ported byte-for-byte from `~/zenda/components/about/`), replacing the old `about/content.tsx` + `config.ts` (deleted — nothing else imported them). It rides the homepage clone's shell verbatim: `.zenda-clone` scope on the route wrapper only (never around `<main>`), `dir="ltr"`, `<ZendaRootScale/>` rendered from the page, fonts from the layout's `next/font` variables. Differences from the homepage: **no `HeroIntro` and no `opacity:0` gate** (the about page has no entrance timeline — its sections animate on scroll), and the reference's `about-page` class (beige `#f4f2ec`) must nest **inside** `.body-v2.bg-beige-home`, not share its div — on the same element `.body-v2.bg-beige-home` outspecifies it and paints `#f5f4ee`. Assets live at `public/images/about/` on the same absolute paths zenda uses. The CSS was already present: `scripts/scope-zenda.mjs` output includes zenda's about styles since the 2026-08-03 regeneration — porting a further zenda page needs **no CSS work** unless zenda's `globals.css` changed after that date (then re-run the script). **Formatter trap, found live:** `prettier-plugin-tailwindcss` strips the leading space inside conditional class strings — `` `x${cond ? " y" : ""}` `` becomes `` `x${cond ? "y" : ""}` `` and the class silently merges into `xy` (this broke `.is-active`/`.is-last`/`.is-game-over`/`.is-top-align` on first render). Write joins with the space outside the interpolation instead: `` `x ${cond ? "y" : ""}`.trim() `` — the pattern zenda-home already uses.
- **`/admissions` speaks zenda's surface language natively (2026-08-05)**: the admissions page is unified with the zenda chrome — full-bleed cream, centered eyebrow+headline headers, flat white `rounded-3xl` cards, a `rounded-[2.5rem]` charcoal CTA panel, a stat trio (Free / 20 min / 2 weeks — the hero's own promises) — but it is **native Tailwind + dictionary keys, deliberately NOT `.zenda-clone` markup**, because this page must stay bilingual/RTL while the clone CSS is LTR-English-only. Mechanics to preserve: (1) the cream reaches the viewport edge via a route-scoped `<style>.main-wrapper{background:#f5f4ee}</style>` in `admissions/page.tsx` (a background inside `.marketing-container` can never escape its max-width — same page-scoped-style mechanic as the homepage's opacity gate); (2) `.band-cream` / `.band-charcoal` in `school-marketing.css` are literal hex pinned to the zenda chrome's values (`#f5f4ee`, `#383940` — the _rendered_ panel color, not the CSS var `#614d76`) with **no `.dark` flip**, and section text pins fixed neutrals for the same reason — the chrome never flips, so theme tokens would invert against it; (3) the page renders no footer of its own — the layout's zenda footer serves (it used to double-render the old map footer); (4) the hero's Lottie ships in Anthropic clay (`#d97757`) over near-black, which clashed with the zenda chrome — `hero-illustration.tsx` walks the parsed JSON and remaps the clay `c.k` value to zenda's `--primary--purple` (`#ac6cf4`) — the same purple the nav's sitename renders in — before handing it to `<Lottie>`. **Only the accent is remapped; the linework keeps the asset's own near-black.** Recoloring it to `--secondary--dark-purple` was tried and reverted: it made the whole illustration monochrome, and zenda's own pairing is purple-on-black. The CDN asset is shared with the Claude API page, so recolor in the client, never re-upload; (5) the admission `SectionContainer` is shared with `academic/` — don't restructure it for this page.

- **`/admissions` header+hero unification (2026-08-06)**: `/en` is the reference and the page now matches it on three axes it was silently off on. (1) **Rail.** `.marketing-container` adds its own inner padding on top of the shared 80rem max-width, which put every section 48px inside the nav logo at 1440 and 12px inside it at 390 — the page read as content pasted under someone else's header. The nav's rail is `.nav_component{padding-inline:5%}` + `.nav_wrap{max-width:80rem;margin-inline:auto}`, so `admissions/page.tsx` reproduces exactly that on its own wrapper in the same route-scoped `<style>` as the cream. Content and logo now share a rail at every width, both edges, both directions; `.marketing-container` itself is untouched for every other page. (2) **Type.** New `.zenda-heading` / `.zenda-body` in `school-marketing.css` mirror zenda's `.heading-style-h1` (DM Sans 500, 4.4/3.25/2.5rem at zenda's own 991/479 breakpoints, `-0.04em`, `#000`) — the hero was Geist 900 at 96px, a different design language two inches under a DM Sans nav. (3) **Buttons.** `.zenda-btn*` + `<ZendaButton>` (`admission/shared/zenda-button.tsx`) are a line-for-line port of zenda's EXPLORE pill, which is **pure CSS, not GSAP** — a 1-cell grid stacking a clipped purple `_bg` over two `_bg-inner` panels parked at -12deg, plus a `data-text` `::after` that rolls in as `_text` rolls out. The nested spans are load-bearing; render it through the component. `.is-alternate` is zenda's own white/purple inverse, used for the secondary CTA so both pills stay one language. **Three traps, all silent:** `var(--font-heading)` resolves EMPTY inside `.page-wrapper`, which invalidates a whole `font-family` list and drops it (the heading then just inherits Geist) — list generic families after `--font-dm-sans` instead; Arabic needs `--font-heading` explicitly (DM Sans has no Arabic and fell through to a platform serif) plus `letter-spacing: 0`, which the display ramp above already documents; and the RTL directional overrides must live **inside** the `no-preference` media query, because `:root[dir=rtl] .zenda-btn_inner::after` ties `.zenda-btn:hover .zenda-btn_inner::after` on specificity (0,3,1) and, being later in the file, wins — the Arabic button hovered to a blank black pill. Playwright defaults to `prefers-reduced-motion: reduce`, so the wipe only shows under `emulateMedia({reducedMotion:'no-preference'})`. Still on the old buttons: the bottom CTA panel.

- **`/admissions` hero copy (2026-08-06)**: headline "A great journey / is about to begin.", lede "Apply in five minutes.", one CTA labelled "Application". Shape notes: the headline breaks where the dictionary's `\n` says it breaks and the component maps over every part (it used to render exactly two spans, which truncated a three-line translation and printed an empty span for a one-line one); `.zenda-lede` is zenda's `.home-hero_para`, a 40px display-scale statement, so the copy has to stay to **one line at 27rem** — the previous three-sentence subtitle ran to eight lines in it; and the hero carries a single action, since the second pill pointed at `/admissions` from inside `/admissions`. **The application promises are stated in four places that must move together** — the stat trio, the hero lede, `marketing.site.admissions.title`, and its decision step. They now read **5 min to apply, 2 days to a decision** (was 20 min / 2 weeks). These are commitments the school has to keep, not decoration; the trio deliberately carries no invented metrics. Dropped in the rewrite and not yet stated anywhere else: applicants can **save an application halfway and resume it** (the wizard does support this) — worth returning to the process section.

- **`/admissions` FAQ section (2026-08-06)**: `admission/sections/faq.tsx` + `faq-accordion.tsx`, sitting between Key Dates and the CTA (answer the objection, then ask — anything after the CTA is read by nobody). It is zenda's `parents/faq.tsx` section **rebuilt native, not ported**: `.zenda-clone` is English/LTR-only, so the geometry is transcribed from `.faq_*` in `zenda-clone.css` (0.875rem gap, 1.5rem corner, `white/40` → solid white when open, 1.75rem/2rem inset, flipping chevron) onto plain Tailwind. Two deliberate divergences from the reference: the open/close is a **`grid-template-rows: 0fr → 1fr` transition instead of GSAP tweening a measured `scrollHeight`** — no measurement to go stale when Arabic reflows the answer to a different height — and each row is a real `<button aria-expanded/aria-controls>`, where zenda binds click to an unreachable `<div>`. **The copy is not new**: `marketing.site.faqs` already held six bilingual admissions questions written for the old homepage template, which the zenda clone replaced on 08-03 — from then until now nothing rendered them. `school-marketing/faqs.tsx` still reads the same keys, so if that homepage template is ever restored the two render the same content on two pages; decide then, don't fork the copy. Only `eyebrow` was added to the key set.

- **`/admissions` Process + Requirements are ONE section (2026-08-06)**: `sections/process.tsx` and `sections/requirements.tsx` are deleted; `sections/journey.tsx` + `journey-switch.tsx` replace them with zenda's `/parents` `p-services` pattern ("Switch to smarter school payments") — a two-column scroll switcher where the start column of steps scrolls past a sticky end column, and only the step crossing the viewport centre, with its paired panel, is shown. **The merge is the point**: two stacked sections asked the reader to hold four steps in their head and then match them against three document groups listed separately; pairing each step with the documents THAT step needs answers "what next" and "what do I bring" in one glance. Geometry is transcribed from `.p-services_*` into `.adm-jrn_*` (school-marketing.css) on logical properties — not the `.zenda-clone` port, English/LTR-only as always. Verified identical to `~/zenda` on :3100 at 1440: grid `624px 624px`, gap 32px, item padding 128px, list padding 48vh, sticky 100vh, panel box **624×567** (ratio 1.10), radius 48px, pill 18px at 8/20px, para 24px. **Two divergences, both forced.** (1) **The panel stacks the step's checklist ABOVE zenda's artwork**, where the reference's sticky column is a product screenshot and nothing else — the checklist is the half of the merge that carries information, so it has to be the thing that switches. Its type is sized UP (title 2rem, items 1.25rem at ≥992) because at body scale the tinted card read two-thirds empty, like a mistake; the art is anchored to the card's foot, as zenda anchors its screenshot (`align-items: flex-end`). **The art is `zenda/public/images/how-it-works/`, NEVER `images/parents/`, and that distinction is the whole reason art was possible here** — the /parents screenshots this very section uses are zenda's PRODUCT UI (`pay-now` is a phone reading "Total Payable USD 200 / PAY NOW / Registration Fees"), which would put a competitor's app on a school's own site AND contradict this block's standing promise that applying is free. `tuition.webp` is out for the same reason: it is a cheque. The `how-it-works` set is generic school objects in zenda's house style — purple over near-black linework, no branding, no UI, no figures. Mapped exams→Submit Application, trips→Campus Tour, counselling→Meet & Greet, uniform→Join Family, each paired with that step's card tint. **Two image traps, both silent, both cost real time.** (a) **Those webps are NOT transparent as shipped** — they carry a transparent MARGIN around an opaque white ground (`exams` is only 7.4% truly transparent), so a corner-pixel check says "transparent" while the card renders an ugly white box. They are flood-filled from the border through both transparent and near-white pixels, then feathered, so interior whites (clipboard paper, certificate face, notepad pages) survive; script kept at `scratchpad/dealpha.py`, sources in `~/zenda` are untouched. (b) **`unoptimized` on the `<Image>` is load-bearing** — Next's AVIF re-encode flattened the alpha on two of the four even after the files were fixed, reintroducing the white box; the files are pre-scaled to 700px/≈45KB so the optimizer has nothing to win anyway. When re-checking any of this, **clear `.next/cache/images` AND the browser cache AND restart dev** — `minimumCacheTTL` is 30 days and stale entries survive all three individually. (2) **No GSAP.** The reference builds a ScrollTrigger per block at `top center`/`bottom center` and repaints on every `onUpdate`; that is just "does this element straddle the centre line", which an IntersectionObserver with `rootMargin: "-50% 0px -50% 0px"` states natively — it fires on crossings, not scroll frames, and unlike a `threshold` (a fraction of the ELEMENT) it does not move with how tall a step gets after translation. **Like the reference, the switch only ever MOVES the active flag and never clears it** — before the first step reaches centre and after the last leaves it nothing straddles the line, and the column must keep showing the nearest step rather than blanking; step 0 ships `is-active` from the server for the same reason. **Below 768px there is no switcher at all** (the reference's `if (window.innerWidth > 767)`): the scrolling column is hidden, panels unstick and stack, and each renders its own copy — which is why the copy is emitted twice, once per column, with exactly one ever visible. The observer is bound to a `matchMedia` change listener, not checked once, or a reader who started narrow never gets it. **The document-to-step mapping invents nothing** — every string already shipped on this page under "Admission Requirements"; only the grouping is new (forms to apply, records to be assessed, personal papers to enrol), and the tour needs nothing, which is said out loud rather than left blank. Dictionary: `process.*` + `requirements.*` collapse into `admission.sections.journey` (net +1 string: `title`, `step`, `whatYouNeed`, `nothingToBring`, `nothingToBringItem` in, four old headers out). Step numerals use `Intl.NumberFormat("ar-u-nu-arab")` — plain `toLocaleString("ar")` yields LATIN digits. Pill and card colors are zenda's own palette pinned as literal hex (`#835bb7` / `#7187c7` / `#c9e584` / `#383940` on `#e3d3ff` / `#ccd5fd` / `#fcd0e5` / `#d9d5ca`) for the reason the rest of this page pins them: the chrome never flips dark, so a theme token would invert against it. **Under `prefers-reduced-motion` the switcher still switches** — it is a scroll-position mapping, not decoration, and freezing it would leave the panel column stuck on one step; only the rise-and-fade entrance is neutralized.
- **`/admissions` Key Dates is zenda's `about-cities` timeline (2026-08-06)**: the four dates moved from a thin vertical rule of 16px badges onto zenda's "Our Growth Journey" pattern — contiguous capsule rows, milestone on the start side, big date pill pinned to the end. **Same four dictionary entries; only the rendering changed.** Geometry is transcribed into `.zenda-tl-*` (school-marketing.css) on logical properties rather than reusing the `.zenda-clone` port at `zenda-about/cities.tsx`, for the usual reason — that scope is English/LTR-only. Verified: rows are 170px tall, identical to the ported `/about`, and mirror exactly under `dir="rtl"` (pill at the left, 16px clear of the content). **Four places where our content forced a divergence from the reference, all because zenda's detail cell holds a two-word fragment ("Dubai, Kochi,") and ours holds a sentence:** (1) the content measure is `min(zenda's cap, 100% + pad − pill − 1rem)` instead of a flat 44rem — at 44rem the description ran clean underneath the pill, which zenda never notices because its text is short; (2) the lead cell keeps its 0.75rem gutter above 479px, or "Regular Deadline" sits flush against the divider rule; (3) below 768px the two cells stack and the divider goes, since the split leaves ~200px at 390px and a one-line description wrapped to five; (4) **rows are deliberately gapless** — measured on `/about`, 170px tall and 170px apart, so adjacent capsules share an edge. That ladder is the design; don't "fix" it with a gap. **The motion is matched to the reference by measurement, not by reading its source** — reading it produced the wrong animation entirely on the first pass (a pill collapsing from full width, which is not what happens). Sampling zenda's `/about-us` on :3100 at 100ms intervals shows: the pill **slides**, at a constant 24rem width, from the start edge to the end edge over 1s while fading beige `#F4F2EC` → white; the content fades in from `x: 8%` over 0.6s; **the two staggers differ** — pill `0.3s × i`, content `0.4s × i + 0.2s` (measured content starts: 200/600/1000/1400/1800ms), because the reference positions the content tween with a relative `-=` offset that shrinks 0.1s per row. Ours reproduces all of it in CSS transitions driven by an IntersectionObserver, verified against the same probe. Two deliberate departures: the slide is `inset-inline-end: calc(100% - var(--tl-pill-w)) → 0`, so the travel is exactly (row − pill) with no measurement and **mirrors under RTL for free** (the reference tweens `left`/`right` in px via GSAP, which cannot mirror — verified 511→1 in Arabic against 1→511 in English); and the reference's ~40px mid-flight width bulge, an artifact of GSAP tweening `left` and `right` at once, is not reproduced. The trigger is `rootMargin: "0px 0px -40%"` to sit on zenda's `start: "top 60%"` — a `threshold` cannot express that line, since a threshold is a fraction of the _element_ and so moved with this list's height and fired early. **The list ships settled and JS adds `pending` on mount**, so a bundle that never runs leaves the dates readable; there is deliberately no failsafe timer, because this is the sixth section and any timer short enough to matter fires before the reader arrives. **That inversion has a StrictMode trap, and it bit (fixed 2026-08-06): `DatesReveal` guarded its effect with an `armed` ref, so under `reactStrictMode`'s mount→cleanup→mount the first pass set `pending` and its cleanup disconnected the observer, then the second pass saw `armed` and returned — leaving the section permanently INVISIBLE, not merely un-animated.** Because the pending state is the hidden one, anything that kills the observer after it is applied strands the content; the effect is now idempotent (no ref) and its cleanup deletes its own `pending`. The mount-time bail also moved from `box.top < innerHeight` to `box.top < innerHeight * 0.6`: testing against mere visibility skipped the animation for a list peeking in at the bottom of the screen, which is a silent no-motion path — the bail must sit on the same 60% trigger line as the observer. Re-verified by driver-side sampling: pill rows start 0/300/600/900ms, content 200/600/1000/1400ms, beige→white over 1s, and 511→1 under RTL. **When probing this with Playwright, scroll with `behavior: 'instant'`** — `<html>` carries `scroll-behavior: smooth`, and a smooth-scrolled probe reads as "the observer never fired". the homepage used to carry Harry Potter copy — Gryffindor/Ravenclaw "houses", Dumbledore as faculty, a trust strip citing the Ministry of Magic — which shipped verbatim on **every tenant's** public website. It is now generic-but-warm copy any school can publish. Two rules follow: (1) never reintroduce a named theme here, because this block renders for all tenants; (2) **never invent a person or an institution** — no named faculty with stock portraits, no fake accreditation logos. Faculty shows departments and figures; the trust strip lists subjects actually taught. A school with no `branding.heroImageUrl` gets a typographic ink panel rather than someone else's campus photo. Design primitives live in `src/styles/school-marketing.css` (`.display-*`, `.eyebrow`, `.section-y`, `.band-*`, `.capsule-*`, `.tile`) — use them instead of raw `text-*`/hex colors. The old version is preserved on the **`hogwarts` branch** and tag `harry-potter-homepage-2026-08-02`.
- **Mobile edge-to-edge uses `--page-px`, not `--marketing-px` (2026-08-02)**: a marketing page carries TWO stacked gutters — the root `.layout-container` (`--container-px`) plus `.marketing-container` (`--marketing-px`). Negating only one leaves a visible strip. `src/styles/container.css` defines `--page-px` as the sum, plus `.bleed-page` (escape to the viewport edge), `.px-page` (realign content inside a bleed), and `.bleed-bg` (paint a sticky header's background full-width via an absolutely positioned `::before`, so the nav stays on the page rails and no scrollable width is added). Prefer these over `.full-bleed` / `100vw` here — `100vw` overshoots by the scrollbar width on desktop.
- **Applying is ALWAYS FREE (2026-06-13)**: The wizard fees step is an informational free-application preview — payment method selection and gateway icons (Bankak/Kashi) are removed. Payment only happens post-acceptance: registration fee on offer acceptance + tuition invoices. Never add a payment gate to the application wizard.
- Single application flow via `application/` (context-based, 5 form steps: attachments → personal → location → academic → fees-preview + submit)
- Campaign-based admission: applications tied to `AdmissionCampaign` records with date windows
- Session-based drafts via `saveApplicationSession` / `resumeApplicationSession`; the apply wizard is auth-gated (login required), while OTP status tracking stays account-less
- OTP status tracking: sha256-hashed OTP; enumeration-oracle closed; atomic attempt counter. Applicants check status via email OTP (no account needed)
- Offer flow: callbackUrl preserves full token'd offer path through login; registration-fee success/fail banners shown post-acceptance
- All public portal writes (inquiry, tour, OTP, submit) are rate-limited
- Tour bookings: TOCTOU-safe seat counter; cancel/reschedule decrements by attendee count; `enableTourBooking` flag gates all entry points
- New-lead notifications (inquiry + tour booking) fire to ADMIN and STAFF roles
- All actions use `getTenantContext()` for schoolId resolution from subdomain
- Homepage sections are composed in `content.tsx` -- order matters for visual flow

## Danger Zones

- `visit/actions.ts` calls `sendEmail()` -- verify template exists in `@/lib/email` before modifying
- Session persistence in `application/` -- breaking auto-save loses applicant progress
- ALL UI text must use dictionary keys -- no hardcoded English. Forms: `dictionary.school.*.form.*`, toasts: `ToastHelper`, validation: `ValidationHelper`, server errors: error codes.

## Related Blocks

- [Dashboard Admission](../school-dashboard/admission/CLAUDE.md) -- admin reviews applications submitted here
- [Application](./application/CLAUDE.md) -- the multi-step application wizard (sub-block)
- [School Dashboard](../school-dashboard/CLAUDE.md) -- admin side of the same school

## After You Finish

1. Update `ISSUE.md` -- check off completed items, add new issues found
2. Update `README.md` -- if file structure changed
3. Run `pnpm tsc --noEmit` to verify no regressions
4. Test: visit `demo.localhost:3000` as anonymous user
