import { Copy } from "lucide-react"
import Link from "next/link"

# Get Started

## 1. The Idea

Building a school automation platform like these real-world products:
- [PowerSchool](https://www.powerschool.com) - $2.8B valuation, 45M students
- [Skyward](https://www.skyward.com) - 5.7M students, 1,900+ districts
- [Infinite Campus](https://www.infinitecampus.com) - 7.8M students, 50 US states
- [Classter](https://www.classter.com) - 500K+ students, 40+ countries

## 2. Run Localhost

Quick setup in 5 minutes:

<div className="space-y-3 mt-4">
  <div className="flex items-center justify-between bg-muted p-3 rounded">
    <code className="text-sm">git clone https://github.com/databayt/hogwarts.git && cd hogwarts</code>
    <button className="p-1 hover:bg-background rounded">
      <Copy className="h-4 w-4" />
    </button>
  </div>

  <div className="flex items-center justify-between bg-muted p-3 rounded">
    <code className="text-sm">pnpm install</code>
    <button className="p-1 hover:bg-background rounded">
      <Copy className="h-4 w-4" />
    </button>
  </div>

  <div className="flex items-center justify-between bg-muted p-3 rounded">
    <code className="text-sm">cp .env.example .env</code>
    <button className="p-1 hover:bg-background rounded">
      <Copy className="h-4 w-4" />
    </button>
  </div>

  <div className="flex items-center justify-between bg-muted p-3 rounded">
    <code className="text-sm">pnpm prisma generate && pnpm prisma migrate dev</code>
    <button className="p-1 hover:bg-background rounded">
      <Copy className="h-4 w-4" />
    </button>
  </div>

  <div className="flex items-center justify-between bg-muted p-3 rounded">
    <code className="text-sm">pnpm dev</code>
    <button className="p-1 hover:bg-background rounded">
      <Copy className="h-4 w-4" />
    </button>
  </div>
</div>

**Running at:** http://localhost:3000

<Link href="/docs/local-setup" className="text-sm text-primary hover:underline">
  → Detailed setup guide with database options
</Link>

## 3. Submit a PR

<div className="space-y-3 mt-4">
  <div className="flex items-center justify-between bg-muted p-3 rounded">
    <code className="text-sm">git checkout -b feature/your-name</code>
    <button className="p-1 hover:bg-background rounded">
      <Copy className="h-4 w-4" />
    </button>
  </div>

  <div className="flex items-center justify-between bg-muted p-3 rounded">
    <code className="text-sm">git add . && git commit -m "feat: add your change"</code>
    <button className="p-1 hover:bg-background rounded">
      <Copy className="h-4 w-4" />
    </button>
  </div>

  <div className="flex items-center justify-between bg-muted p-3 rounded">
    <code className="text-sm">git push origin feature/your-name</code>
    <button className="p-1 hover:bg-background rounded">
      <Copy className="h-4 w-4" />
    </button>
  </div>
</div>

**Checklist:**
- ✅ TypeScript passes (`pnpm tsc --noEmit`)
- ✅ Tests pass (`pnpm test`)
- ✅ Build succeeds (`pnpm build`)
- ✅ Database queries include `schoolId`

<Link href="/docs/contributing" className="text-sm text-primary hover:underline">
  → Full contribution guide
</Link>

## 4. First Contribution

Add your name to our contributors showcase:

1. **Create a new file:** `src/app/[lang]/(marketing)/blog/contributors/YOUR-NAME.mdx`

2. **Add your info:**
```mdx
---
name: Your Name
github: your-github-username
date: 2024-11-21
---

# Your Name

Hi! I'm contributing to Hogwarts because [your reason].
```

3. **Submit PR** with title: `feat: add [Your Name] to contributors`

That's it! Welcome to Hogwarts 🎉