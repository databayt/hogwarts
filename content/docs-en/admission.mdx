---
title: Admission
description: Admin-side admission management — application review, enrollment confirmation, merit list, section placement, and document-AI processing.
---

The admin admission flow runs from application review through enrollment confirmation. Confirming enrollment fires a single 30-second transaction that creates the Student, Guardians, FeeAssignments, and UserInvoices in one atomic step.

**Applying is always free.** The application wizard collects information only — no payment is taken at the application stage. Payment is triggered at two later points: the registration fee when the applicant accepts an offer, and tuition invoices after enrollment is confirmed.

## Pipeline

```
Applicant submits (free) → SUBMITTED
Review application       → UNDER_REVIEW
Score & rank             → Merit list
Select applicant         → SELECTED (admissionOffered=true)
  Applicant accepts offer → pays registration fee
Confirm enrollment       → ADMITTED
                           → Student (ACTIVE)
                           → User (role=STUDENT)
                           → Guardians + StudentGuardian
                           → StudentYearLevel + AcademicGrade
                           → FeeAssignments + UserInvoices (tuition)
                           → StudentDocuments
Place in section         → Classroom roster
Edit in wizard           → Update details
```

## Routes

| Tab          | Route                                 | Purpose                                                |
| ------------ | ------------------------------------- | ------------------------------------------------------ |
| Campaigns    | `/{lang}/admission`                   | Create / manage admission campaigns                    |
| Applications | `/{lang}/admission/applications`      | Review submitted applications                          |
| Merit list   | `/{lang}/admission/merit`             | Rank applications by scores                            |
| Enrollment   | `/{lang}/admission/enrollment`        | Confirm enrollment for selected apps                   |
| Leads        | `/{lang}/admission/leads`             | Inquiries + tour bookings (ADMIN/ACCOUNTANT read-only) |
| Settings     | `/{lang}/admission/settings`          | Configure admission portal                             |
| Detail       | `/{lang}/admission/applications/{id}` | Single application view                                |

## Status flow

| Status                | Trigger                      | What happens                                      |
| --------------------- | ---------------------------- | ------------------------------------------------- |
| `DRAFT`               | Session created              | Form data being collected                         |
| `SUBMITTED`           | Applicant submits            | Application number generated, email sent          |
| `UNDER_REVIEW`        | Admin updates                | `reviewedAt` + `reviewedBy` set                   |
| `SHORTLISTED`         | Admin updates                | Applicant notified                                |
| `ENTRANCE_SCHEDULED`  | Admin schedules              | Entrance exam date set                            |
| `INTERVIEW_SCHEDULED` | Admin schedules              | Interview date set                                |
| `SELECTED`            | Admin selects                | Offer created (configurable expiry), payment link |
| `WAITLISTED`          | Admin waitlists              | Applicant notified                                |
| `REJECTED`            | Admin rejects                | Applicant notified                                |
| `ADMITTED`            | Admin confirms enrollment    | Student + Guardians + Fees + Invoices created     |
| `WITHDRAWN`           | Applicant or admin withdraws | Application closed                                |

## Enrollment transaction

`confirmEnrollment()` runs a single 30-second `db.$transaction`:

1. Set application status `ADMITTED` with enrollment number `ENR-{timestamp}-{random}`.
2. Create `User` (role `STUDENT`) if applicant was a guest; promote `USER` → `STUDENT` otherwise.
3. Create `Student` with application data pre-populated; match `applyingForClass` → `YearLevel` (exact name → case-insensitive → grade number → `levelOrder`); create `StudentYearLevel` and set `academicGradeId`.
4. Auto-assign active `FeeStructures` for the campaign's academic year → `FeeAssignment` rows (`PENDING`).
5. Create `Guardian` + `StudentGuardian` + `GuardianPhoneNumber` from father / mother data; copy application documents to `StudentDocument`.
6. Generate one `UserInvoice` per `FeeAssignment` via `createInvoiceFromEnrollment()` (non-fatal): number `ENR-{unique}`, status `UNPAID`, due 30 days; one `UserInvoiceItem` per fee structure.

Post-transaction (best-effort): notify student + guardians (in-app + email), dispatch `fee_due` notifications, return `studentId`. The student is created with `wizardStep: null` and `status: ACTIVE`.

## Application → Student data transfer

| Application                                       | Student                            | Notes                              |
| ------------------------------------------------- | ---------------------------------- | ---------------------------------- |
| firstName / middleName / lastName                 | same                               | Direct copy                        |
| dateOfBirth, gender, nationality                  | same                               | Direct copy                        |
| email                                             | email                              | Direct copy                        |
| phone / alternatePhone                            | mobileNumber / alternatePhone      | Direct copy                        |
| address, city, state, postalCode, country         | currentAddress, …                  | Direct copy                        |
| photoUrl                                          | profilePhotoUrl                    | Profile picture                    |
| previousSchool / previousClass                    | previousSchoolName / previousGrade | Direct copy                        |
| previousMarks + previousPercentage + achievements | previousAcademicRecord             | Combined string                    |
| fatherName / motherName                           | Guardian record                    | Creates Guardian + StudentGuardian |
| fatherPhone / guardianPhone                       | emergencyContactPhone              | First available used               |
| guardianRelation                                  | emergencyContactRelation           | Falls back to "Parent"             |
| documents (JSON)                                  | StudentDocument records            | Each document creates a row        |
| applyingForClass                                  | academicGradeId                    | Via YearLevel → AcademicGrade      |

## Guardian auto-creation

For each parent (father, mother):

1. Split name into `firstName` + `lastName` (last space).
2. Upsert `GuardianType` (`father`, `mother`).
3. Create `Guardian` (or find by email).
4. Create `StudentGuardian` join row with occupation, `isPrimary`.
5. Create `GuardianPhoneNumber` if phone provided.

Father is primary; if no father, mother becomes primary.

## Section placement

After enrollment, place the student in a section:

- Enrollment response includes `suggestedSectionId` if exactly one section has capacity for the student's grade.
- Lists sections with available capacity for that grade.
- `placeStudentInSection()` assigns + auto-enrols in grade classes via `enrollStudentInGradeClasses()`.

## Wizard editing

Enrolled students can be edited via the student wizard (Students table → Actions → Edit). All application data is pre-populated.

`updateStudentWizardStep` only updates `wizardStep` for draft students (non-null `wizardStep`) — prevents accidental reversion of enrolled students. Final button reads "Save", not "Create".

## Merit list

Admins enter entrance and interview scores directly on the merit table (0–100 per score). `generateMeritList()` computes a weighted `meritScore` for each application:

- Entrance exam — 60%
- Interview — 40%

Applications are ranked by `meritScore` descending; unscored applications sort last. Scores are batch-written via `updateApplicationScores()`. The merit table covers shortlisted, selected, and waitlisted applications.

## Offer → accept → pay → enroll flow

Once an applicant is `SELECTED`:

1. An offer is created with a configurable expiry (`offerExpiryDays`, default 14).
2. The applicant logs in via a token'd offer URL (login preserves the full callback path).
3. The applicant pays the **registration fee** through the school's gateway (Tap for UAE/Gulf, Stripe elsewhere). The application wizard shows an informational payment preview — no payment is collected at the application stage.
4. On payment success, the admin sees a registration-fee paid banner and can proceed.
5. Admin clicks "Confirm enrollment" → `confirmEnrollment()` transaction fires (see Enrollment transaction).
6. Post-enrollment the admin uses the **Placement dialog** on the enrollment table to pick a section (seat counts shown); `placeStudentInSection()` enrols the student in all grade classes.

Abandoned-checkout retry is unblocked — applicants who navigated away can return and complete payment.

## Leads tab

The **Leads** tab (`/{lang}/admission/leads`) surfaces:

- **Inquiries** — contact form submissions with name, email, phone, interest grade, source, and follow-up status.
- **Tour bookings** — scheduled visits with attendee count, status (`PENDING` / `CONFIRMED` / `CANCELLED`), and reschedule/cancel actions.

ADMIN and STAFF roles have full action access; ACCOUNTANT has read-only access. New inquiries and bookings trigger in-app notifications to ADMIN + STAFF. Tour bookings guard against overselling (TOCTOU-safe) and honour the `enableTourBooking` campaign flag; cancel/reschedule decrements the attendee count correctly.

## Document-AI processing

Uploaded applicant documents (ID scans, transcripts) are processed by an extraction queue. The cron `/api/cron/process-document-jobs` runs every 10 minutes and drains pending jobs. `classifyDocument()` and `getDocumentProcessingStatus()` are RBAC-gated. AI usage is budget-checked (`canUseAI`) before each call; the bank-receipt Zod schema is optional so partial extractions do not fail the whole job.

## Settings

```typescript
{
  allowMultipleApplications: false,
  requireDocuments: true,
  // applicationFee removed — applying is always free
  offerExpiryDays: 14,           // 1-90
  autoEmailNotifications: true,
  enableOnlinePayment: false,
  paymentMethods: ["stripe", "cash"],
  bankDetails: { bankName, accountName, accountNumber, iban, swiftCode },
  cashPaymentInstructions: "",
  entranceWeight: 60,            // merit weights (sum=100)
  interviewWeight: 40,
}
```

## Server actions (admin, auth + permission)

| Action                                                                          | Permission                      |
| ------------------------------------------------------------------------------- | ------------------------------- |
| `getApplications()`                                                             | viewApplications                |
| `updateApplicationStatus()`                                                     | updateStatus                    |
| `confirmEnrollment()`                                                           | confirmEnrollment               |
| `placeStudentInSection()`                                                       | confirmEnrollment               |
| `getAvailableSectionsForPlacement()`                                            | confirmEnrollment               |
| `generateMeritList()`                                                           | manageMerit                     |
| `updateApplicationScores()`                                                     | manageMerit                     |
| `getCampaigns()` / `createCampaign()` / `updateCampaign()` / `deleteCampaign()` | viewCampaigns / manageCampaigns |
| `getAdmissionSettings()` / `saveAdmissionSettings()`                            | manageSettings                  |
| `fetchCampaignOptions()`                                                        | viewCampaigns                   |
| `classifyDocument()` / `getDocumentProcessingStatus()`                          | viewApplications (RBAC-gated)   |

## Multi-tenant safety

All queries scoped by `schoolId`. Submit flow resolves school from subdomain via `getSchoolBySubdomain()`; session is verified to belong to that school.

## Key files

| Purpose              | Path                                                                                                                                                     |
| -------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Dashboard actions    | `src/components/school-dashboard/admission/actions.ts`                                                                                                   |
| Applications surface | `src/components/school-dashboard/admission/applications-{table,columns}.tsx` + `application-detail-content.tsx`                                          |
| Enrollment / merit   | `src/components/school-dashboard/admission/{enrollment,merit}-table.tsx`                                                                                 |
| Leads tab            | `src/components/school-dashboard/admission/leads/`                                                                                                       |
| Document AI          | `src/components/school-dashboard/admission/ai/{classify,bank-receipt-schema}.ts`                                                                         |
| Settings             | `src/components/school-dashboard/admission/settings/{actions,validation}.ts`                                                                             |
| Invoice utility      | `src/components/school-dashboard/finance/invoice/actions.ts`                                                                                             |
| Schema               | `prisma/models/admission.prisma`                                                                                                                         |
| Tests                | `src/components/school-dashboard/admission/__tests__/{actions,enrollment-pipeline}.test.ts` + `finance/invoice/__tests__/create-from-enrollment.test.ts` |

## See also

- [Application](/docs/application) — applicant-side flow
- [Multi-tenancy](/docs/multi-tenancy) — `schoolId` scoping
- [Invoice](/docs/invoice) — auto-generation from enrollment
- [Fees](/docs/fees) — fee assignment lifecycle
