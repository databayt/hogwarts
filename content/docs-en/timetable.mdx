---
title: "Timetable"
description: "Complete guide to the timetable module - structure catalog, role-based views, slot management, AI generation, conflict detection, substitutions, templates, analytics, and export"
---

## Overview

The Timetable module handles school schedule management end-to-end: choosing a timetable structure during onboarding, configuring working days and periods, assigning weekly slots, detecting conflicts, managing substitutions, AI-powered generation, PDF export, and role-specific views. All operations are tenant-scoped by `schoolId`.

### Module Flow

### Permissions

Role-based access via `permissions-config.ts`:

| Role       | Access Level           | Can Modify | Can Export | Can Manage Conflicts | Can Configure Settings |
| ---------- | ---------------------- | ---------- | ---------- | -------------------- | ---------------------- |
| DEVELOPER  | Full                   | Yes        | Yes        | Yes                  | Yes                    |
| ADMIN      | Full                   | Yes        | Yes        | Yes                  | Yes                    |
| ACCOUNTANT | Read Only (Admin grid) | No         | No         | No                   | No                     |
| STAFF      | Read Only (Admin grid) | No         | No         | No                   | No                     |
| TEACHER    | Read + Own             | No         | Yes        | No                   | No                     |
| STUDENT    | Read Only              | No         | Yes        | No                   | No                     |
| GUARDIAN   | Read Only              | No         | No         | No                   | No                     |

All operations are tenant-scoped by `schoolId`.

## Routes and Navigation

### Layout Tabs

The timetable `layout.tsx` renders role-based tabs via `PageNav`:

- **Admin tabs**: Overview, Analytics, Generate, Conflicts, Settings
- **Non-admin tabs**: Today, Full Week

### Route Map

| Route                   | Page Component              | Access Roles                 |
| ----------------------- | --------------------------- | ---------------------------- |
| `/timetable`            | `TimetableContent`          | ADMIN, TEACHER, DEVELOPER    |
| `/timetable/full`       | `TimetableContent` (full)   | ADMIN, TEACHER, DEVELOPER    |
| `/timetable/by-class`   | `TimetableByClassContent`   | ADMIN, DEVELOPER             |
| `/timetable/by-teacher` | `TimetableByTeacherContent` | ADMIN, DEVELOPER             |
| `/timetable/by-room`    | `TimetableByRoomContent`    | ADMIN, DEVELOPER             |
| `/timetable/conflicts`  | `TimetableConflictsContent` | ADMIN, DEVELOPER             |
| `/timetable/generate`   | `GenerateTimetableContent`  | ADMIN, DEVELOPER             |
| `/timetable/templates`  | `TemplatesContent`          | ADMIN, DEVELOPER             |
| `/timetable/analytics`  | `TimetableAnalyticsContent` | ADMIN, DEVELOPER             |
| `/timetable/settings`   | `TimetableSettingsContent`  | ADMIN, DEVELOPER             |
| `/my-timetable`         | (Student/Guardian route)    | STUDENT, GUARDIAN, DEVELOPER |

Every route has `error.tsx` and `loading.tsx` boundaries.

**Orphaned routes**: The `by-class`, `by-teacher`, `by-room`, and `templates` routes have page components but are **not linked** in the layout tabs or sidebar navigation. They're only accessible via direct URL.

### Sidebar

Timetable appears in the platform sidebar for ADMIN, TEACHER, and STUDENT roles with `href: "/timetable"` and icon `clock`.

---

## Role-Based Experience

The production entry point is `content-production.tsx` which wraps `RoleRouter` in a `SessionProvider`. The `RoleRouter` calls `getPersonalizedTimetable()` to determine `viewType` and routes to the appropriate view.

### Data Flow Per Role

| Role       | View Component | Data Function                                  | What They See                                         | Known Bugs                                        |
| ---------- | -------------- | ---------------------------------------------- | ----------------------------------------------------- | ------------------------------------------------- |
| DEVELOPER  | `AdminView`    | `getTimetableByRoom` / `getTimetableByTeacher` | Room/Teacher grid, editable slots, slot editor        | No class/grade filter mode                        |
| ADMIN      | `AdminView`    | Same as DEVELOPER                              | Same as DEVELOPER                                     | Same                                              |
| TEACHER    | `TeacherView`  | `getTimetableByTeacher`                        | Personal schedule, workload stats, current/next class | No export button (despite `canExport` permission) |
| STUDENT    | `StudentView`  | `getTimetableByStudentGrade`                   | All enrolled subjects (grade-based), PDF export       | None                                              |
| GUARDIAN   | `GuardianView` | `getChildTimetable`                            | Child selector + all enrolled classes per child       | None                                              |
| ACCOUNTANT | `AdminView`    | Same as ADMIN                                  | Full admin view, read-only (`editable={false}`)       | None                                              |
| STAFF      | `AdminView`    | Same as ADMIN                                  | Full admin view, read-only (`editable={false}`)       | None                                              |

### How Routing Works

1. `getPersonalizedTimetable()` reads the user's role from session
2. Role → `viewType` mapping in a switch statement, plus `editable` flag (default `true`)
3. ACCOUNTANT and STAFF get `viewType = "admin"` with `editable = false`
4. `RoleRouter` renders the matching view component, passing `editable` to `AdminView`
5. `AdminView` passes `editable` prop to `SimpleGrid` and conditionally disables `onSlotClick`

---

## Grade-Based Filtering

### Student Grade Resolution Chain

The student view resolves a student's timetable through enrollment records:

```
User (session)
  → Student (db.student.findFirst where userId)
    → StudentClass (db.studentClass.findMany where studentId)
      → classIds[]
        → Timetable (db.timetable.findMany where classId IN classIds)
```

Grade name comes from a separate chain:

```
Student → StudentYearLevel → YearLevel.levelName

---

## Onboarding: Schedule Step

The schedule step is inserted between **Capacity** (order 5) and **Branding** (order 6) in the onboarding flow.

### Behavior

1. Reads the school's `country`, `schoolType`, and `schoolLevel` from saved data.
2. Calls `getRecommendedStructures()` with those inputs.
3. Shows recommended structures at top with visual timeline preview, alternatives below.
4. User picks one (auto-saves to `school.system` field) or skips to customize later.
5. The step is **optional** - the "Next" button is always enabled.

### Structure Preview Component

Each structure card shows:

- Name and description
- Period count, time range, working days
- Color-coded visual timeline bar (blue = class, amber = break, green = lunch)
- "Recommended" badge on the default structure

### Key Files

| File                                                       | Purpose                                           |
| ---------------------------------------------------------- | ------------------------------------------------- |
| `src/app/[lang]/onboarding/[id]/schedule/page.tsx`         | Route page                                        |
| `src/components/onboarding/schedule/content.tsx`           | Main step UI                                      |
| `src/components/onboarding/schedule/structure-preview.tsx` | Visual timeline component                         |
| `src/components/onboarding/schedule/actions.ts`            | `getSchoolScheduleData()`, `saveScheduleChoice()` |
| `src/components/onboarding/schedule/config.ts`             | Step constants                                    |
| `src/components/onboarding/schedule/validation.ts`         | Zod schema                                        |
| `src/components/onboarding/schedule/config-row.tsx`        | Config row display component                      |

---

## Applying a Structure

The `applyTimetableStructure()` server action creates Period records from a structure definition and updates the school's week configuration.

### What It Does

1. Validates admin access and tenant context.
2. Looks up the structure by slug (supports legacy template names via `LEGACY_TEMPLATE_MAP`).
3. Optionally deletes existing periods for the year (`replaceExisting: true`).
4. Creates `Period` records for each entry in the structure (class, break, and lunch periods).
5. Upserts `SchoolWeekConfig` with the structure's working days and lunch position.
6. Logs the action for audit trail.

### Usage

```typescript
// From timetable settings (replacing existing periods)
await applyTimetableStructure({
  yearId: "clx...",
  structureSlug: "sd-gov-default",
  replaceExisting: true,
})

// Legacy template names still work
await applyTimetableStructure({
  yearId: "clx...",
  structureSlug: "standard_8", // mapped to sd-gov-default
})