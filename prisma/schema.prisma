datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
  engineType = "binary"
}

// Multi-file schema configuration
// All .prisma files in the prisma/models directory are automatically included
// when using "schema": "./prisma" in package.json
