datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
  engineType = "binary"
}

// Import model files
// Using Prisma's built-in support for multiple files via VSCode/cursor context; the actual schema is composed by concatenation at build time.
// Ensure dev tooling picks up all model files.
